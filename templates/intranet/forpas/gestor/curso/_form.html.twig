{{ form_start(form) }}
    {{ form_widget(form) }}

    <p class="text-muted mt-3">* Campos obligatorios</p>

    <button class="btn btn-primary"><span class="fas fa-save"></span> {{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}