{{ form_start(form) }}
    {{ form_widget(form) }}
    <p class="text-muted mt-3">* Campos obligatorios</p>
    <!-- Botón "Volver" -->
    <a class="btn btn-primary" href="#"
       onclick="window.location.href='{{ path('intranet_forpas_gestor_formador_index') }}';">
        <span class="fas fa-reply"></span> Volver
    </a>
    <!-- Botón "Guardar" -->
    <button class="btn btn-primary">
        <span class="fas fa-save"></span> {{ button_label|default('Guardar') }}
    </button>
{{ form_end(form) }}